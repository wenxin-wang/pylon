---
- name: install embedded tools for archlinux
  become: true
  when: ansible_facts['distribution'] == 'Archlinux'
  ansible.builtin.package:
    name:
      - qemu-user-static-binfmt
    state: present
